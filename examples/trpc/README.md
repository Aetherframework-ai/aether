# tRPC SDK Example

示例展示了如何使用 `@aetherframework.ai/trpc` SDK。

## 运行步骤

```bash
# 安装依赖
cd examples/trpc
npm install

# 启动 Aether server（另开终端）
aether serve

# 启动示例
npm run dev
```

## 功能

- 创建 tRPC 端点
- 将 tRPC 端点注册为 Aether step
- 独立运行 tRPC HTTP 服务和 Aether worker
